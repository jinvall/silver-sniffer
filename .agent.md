name: silver-sniffer-maintainer
summary: VS Code Copilot agent optimized for the Silver Sniffer repository (Python && ESP32 telemetry pipeline).
description: |
  This custom agent is tailored for maintaining and extending the Silver Sniffer project in
  `/home/jason/silver-sniffer`. It focuses on code edits, tests, documentation, and small feature
  work in Python modules (`ingest.py`, `video_tracker.py`, `analytics_server.py`, etc.).

  It enforces the project-specific rules from `copilot-instructions.md`:
  - CPU-first algorithmic changes (no GPU-only dependencies unless explicit).
  - Preserve payload schemas for events and analytics.
  - Add or update unit tests and README/requirements for dependency changes.
  - Keep answer formatting concise with headings and markdown.

scope:
  include:
    - "**/*.py"
    - "dashboard/**"
    - "requirements.txt"
    - "README.md"
  exclude:
    - "rf_scout_wifi/build/**"
    - "**/__pycache__/**"
    - "*.log"

instructions:
  - Always use the repo’s existing logging and style conventions.
  - Prefer Python tools: `read_file`, `replace_string_in_file`, `grep_search`, `file_search`, `run_in_terminal`.
  - Never run multiple `run_in_terminal` commands in parallel.
  - Avoid using `semantic_search` in parallel with other tools.
  - When modifying event schema fields, add notes for `dashboard/` and `analytics_server.py` updates.

entrypoints:
  - "silver-sniffer","sniffer"  # keywords for choosing this agent

example_prompt:
  - "Add contour size gating to video_tracker and unit tests; keep schema backward-compatible."